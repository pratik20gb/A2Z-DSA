#include <bits/stdc++.h>
using namespace std;

#define int long long
#define fast                 \
    ios::sync_with_stdio(0); \
    cin.tie(0);

int32_t main()
{
    fast;
    int m, n;
    cin >> m >> n;
    int maxDomino = (m * n) / 2;
    cout << maxDomino << endl;
    return 0;
}
