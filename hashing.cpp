#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    // precomputaction
    int hash[13];
    


    int q;
    cin >> q;
    while (q--)
    {
        int number;
        cin >> number;
        // fetch
    }
    return 0;
}